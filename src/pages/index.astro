---
// Root path: redirect to language by domain (.com → /en/, .cn → /zh/)
// Rendered as minimal HTML + client-side redirect so both domains share the same dist.
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Moments+</title>
    <meta name="robots" content="noindex" />
  </head>
  <body>
    <script>
      (function () {
        var host = window.location.hostname;
        var path = window.location.pathname;
        var search = window.location.search || "";
        var hash = window.location.hash || "";

        if (path === "/" || path === "") {
          var targetLang = "en";
          if (host.endsWith(".cn")) {
            targetLang = "zh";
          }
          var target = "/" + targetLang + "/" + search + hash;
          window.location.replace(target);
        }
      })();
    </script>
    <p>Redirecting…</p>
  </body>
</html>
